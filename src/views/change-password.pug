extend base.pug

block content 
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(placeholder="Password", name="Password", type="text", required)
        input(placeholder="Password Confirmation", name="Password1",  type="text", required)
        input(type="Submit" value="Change Password")
